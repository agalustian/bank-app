apiVersion: v1
kind: Pod
metadata:
  name: healthtest
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['bank-notifications:8082/actuator/health']
  restartPolicy: Never