import org.openapitools.generator.gradle.plugin.tasks.GenerateTask

plugins {
    id "java"
    id "org.springframework.boot" version "3.5.6"
    id "io.spring.dependency-management" version "1.1.7"
    id "org.openapi.generator" version "7.12.0"
}

group = "ru.bank"
version = "0.0.1"
description = "Bank front ui application"

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

sourceSets.main.java.srcDir "$projectDir/build/generated/src/main/java"

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-actuator'
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-thymeleaf'

    testImplementation 'org.springframework.boot:spring-boot-starter-test'

    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

tasks.named("test") {
    useJUnitPlatform {
        filter {
            exclude("**/integration/**")
        }
    }
    testLogging {
        events "passed", "skipped", "failed"
    }
}

tasks.register("integrationTest", Test) {
    useJUnitPlatform {
        filter {
            include("**/integration/**")
        }
    }
    testLogging {
        events "passed", "skipped", "failed"
    }
}

//tasks.register("buildGatewayClient", GenerateTask) {
//    generatorName.set("java")
//    inputSpec.set("$projectDir/src/main/resources/gateway.json")
//    outputDir.set("$projectDir/build/generated")
//    modelPackage.set("ru.bank.gateway.domain")
//    invokerPackage.set("ru.bank.gateway.client")
//    apiPackage.set("ru.bank.gateway.client.api")
//    packageName.set("Notifications")
//    configOptions.set([
//            hideGenerationTimestamp: "true",
//            library                : "resttemplate",
//            useJakartaEe           : "true",
//            useTags                : "true",
//            openApiNullable        : "false",
//            serializableModel      : "true"
//    ])
//}
//
//compileJava.dependsOn tasks.buildGatewayClient

tasks.register("buildAccountsClient", GenerateTask) {
    generatorName.set("java")
    inputSpec.set("$projectDir/src/main/resources/accounts.json")
    outputDir.set("$projectDir/build/generated")
    modelPackage.set("ru.bank.front.accounts.domain")
    invokerPackage.set("ru.bank.front.accounts.client")
    apiPackage.set("ru.bank.front.accounts.client.api")
    packageName.set("Accounts")
    configOptions.set([
            hideGenerationTimestamp: "true",
            library                : "resttemplate",
            useJakartaEe           : "true",
            useTags                : "true",
            openApiNullable        : "false",
            serializableModel      : "true"
    ])
}

compileJava.dependsOn tasks.buildAccountsClient

tasks.register("buildTransferClient", GenerateTask) {
    generatorName.set("java")
    inputSpec.set("$projectDir/src/main/resources/transfer.json")
    outputDir.set("$projectDir/build/generated")
    modelPackage.set("ru.bank.front.transfer.domain")
    invokerPackage.set("ru.bank.front.transfer.client")
    apiPackage.set("ru.bank.front.transfer.client.api")
    packageName.set("Transfer")
    configOptions.set([
            hideGenerationTimestamp: "true",
            library                : "resttemplate",
            useJakartaEe           : "true",
            useTags                : "true",
            openApiNullable        : "false",
            serializableModel      : "true"
    ])
}

compileJava.dependsOn tasks.buildTransferClient

tasks.register("buildCashClient", GenerateTask) {
    generatorName.set("java")
    inputSpec.set("$projectDir/src/main/resources/cash.json")
    outputDir.set("$projectDir/build/generated")
    modelPackage.set("ru.bank.front.cash.domain")
    invokerPackage.set("ru.bank.front.cash.client")
    apiPackage.set("ru.bank.front.cash.client.api")
    packageName.set("Cash")
    configOptions.set([
            hideGenerationTimestamp: "true",
            library                : "resttemplate",
            useJakartaEe           : "true",
            useTags                : "true",
            openApiNullable        : "false",
            serializableModel      : "true"
    ])
}

compileJava.dependsOn tasks.buildCashClient